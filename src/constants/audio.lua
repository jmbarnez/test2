-- ============================================================================
-- AUDIO CONFIGURATION
-- ============================================================================

return {
    master_volume = 1.0,
    music_volume = 0.7,
    sfx_volume = 0.8,
    max_simultaneous_sounds = 32,
    sound_falloff_distance = 1500,
    sound_max_distance = 3000,
}
